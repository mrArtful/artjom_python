# Count vowels
# Write a function count_vowels(text) that returns the number of vowel characters (a e i o u) in the string, case-insensitive.


# Expected output

# count_vowels("Hello World") → 3
# count_vowels("Python") → 2
# count_vowels("AEIOU") → 5




# Return multiple values
# Write a function stats(numbers) that takes a list of numbers and returns three values: the minimum, maximum, and average (rounded to 2 decimal places).

# Hint: Python functions can return multiple values as a tuple — return a, b, c

# Expected output

# stats([4, 1, 7, 2, 9]) → (1, 9, 4.6)
# mn, mx, avg = stats([10, 20, 30])
# mn → 10, mx → 30, avg → 20.0




# Format a price
# Write a function format_price(amount, currency="EUR", decimals=2) that returns a formatted price string. The currency symbol should appear before the amount.

# Expected output

# format_price(9.5) → "EUR 9.50"
# format_price(1999, currency="USD") → "USD 1999.00"
# format_price(3.14159, decimals=3) → "EUR 3.142"




# Repeat a string
# Write a function repeat(text, times=2, separator="") that repeats text a given number of times, joined by separator.

# Expected output

# repeat("ha") → "haha"
# repeat("go", times=3, separator="-") → "go-go-go"
# repeat("hi", separator=", ", times=4) → "hi, hi, hi, hi"



